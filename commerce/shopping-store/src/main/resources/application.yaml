spring:
  application:
    name: shopping-store
  config:
    import: configserver:http://localhost:34735
  cloud:
    openfeign:
      enabled: false
    discovery:
      enabled: true
      serviceId: config-server
    fail-fast: true
    retry:
      useRandomPolicy: true
      max-interval: 6000