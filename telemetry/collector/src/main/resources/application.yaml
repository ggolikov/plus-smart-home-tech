spring:
  application:
    name: collector
  config:
    import: configserver:http://localhost:34735
  cloud:
    config:
      fail-fast: true
      retry:
        useRandomPolicy: true
        max-interval: 6000